<%= add_gritter(flash.now[:errors], :image => :error, :title => "Pay attention!", :sticky => true) %>
$('#header').html('<%= escape_javascript(render(partial: 'layouts/menu')) %>');
$.fancybox.close()
